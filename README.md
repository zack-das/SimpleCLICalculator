A simple CLI python calculator.
Run python calculator.py to start you calculator .
!!!!It is not recommended to use Python's eval() function primarily because of the significant security risks it poses and its potential to introduce hard-to-debug vulnerabilities and performance issues [1]. 
